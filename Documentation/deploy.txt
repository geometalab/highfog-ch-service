Create file 'ext_config.py' in the same dir as config.py
Add following lines:
FTP_URL = '<<FTP Server URL>>'
FTP_USER = '<<FTP login Username>>'
FTP_PW = '<<FTP login password>>'

FORECAST_INTERVAL = 3

DATABASE_URL = '<<Full database URL with username and password for productive database>>'
TEST_DATABASE_URL = '<<Full database URL with username and password for test database>>'
EOSM_LOGIN = '<<EOSMdb credentials in following format (see IFS wiki for credentials): "host='' port='' dbname='gis_db' user='' password=''">>'

ELEVATION_SERVICE_URL = '<<URL to the elevation service>>'